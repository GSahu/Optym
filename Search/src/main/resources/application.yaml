server:
  port: 8081
  servlet:
    context-path: /jokesearch/api
searcher:
  ui:
    url: http://localhost:8082
spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:tcp://localhost:9090/mem:mydb
    #url: jdbc:h2:mem:mydb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update